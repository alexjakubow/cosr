% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.r
\name{summary_longform}
\alias{summary_longform}
\title{Transform summary data into long format for reporting}
\usage{
summary_longform(tbl, ...)
}
\arguments{
\item{tbl}{A data frame containing summary data with a \code{date} column and
various metric columns.}

\item{...}{Optional grouping variables to include in the transformation. If
provided, the function will group the data by these variables along with
the metric. If not provided, the function will group only by the metric.}
}
\value{
A data frame in long format with columns for \code{date}, \code{metric},
\code{value}, \code{moment}, and any specified grouping variables. The \code{moment}
column distinguishes between:
\itemize{
\item \code{value}: the observed metric value at that date
\item \code{lag}: the value from the previous period
\item \code{delta}: the absolute change from the previous period
\item \code{growth}: the relative change (rate) from the previous period
}
}
\description{
This function takes a summary data frame and transforms it into a long format
suitable for reporting. It captures grouping variables, pivots the data to a
long format, and calculates changes over time (lag, delta, growth) for each
metric.
}
\examples{
\dontrun{
# No grouping
summary_longform(los_ts_summary)

# One grouping variable
summary_longform(los_ts_funded, funded)

# Multiple grouping variables
summary_longform(los_ts_data, funded, institution)
}

}
\seealso{
\code{\link[=generate_value_types]{generate_value_types()}}, \code{\link[=generate_reporting_dataset]{generate_reporting_dataset()}}
}
