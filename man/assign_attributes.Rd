% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.r
\name{assign_attributes}
\alias{assign_attributes}
\title{Assign current attributes (subgroup status) for all OSRs}
\usage{
assign_attributes(sample = cosr::expr_valid_regs)
}
\arguments{
\item{sample}{A dplyr filter expression to apply to the base table of OSRs.
Defaults to \code{cosr::expr_valid_regs}, which captures all "valid" registrations.}
}
\value{
Invisible NULL. Results are written to Parquet files in data/:
\itemize{
\item data/osr_affiliated.parquet
\item data/osr_funder.parquet
\item data/osr_funded.parquet
\item data/osr_institution.parquet
\item data/osr_template.parquet
\item data/osr_provider.parquet
\item data/osr_subject.parquet
\item data/osr_subject_parent.parquet
}
}
\description{
This is typically the FIRST STEP in a workflow. It captures current
attributes/subgroup memberships for all Open Science Registrations (OSRs)
and caches the results as Parquet files in the \verb{data/} directory.
}
\details{
The function processes all 8 supported attributes in parallel and saves
them for use in downstream analyses.
}
\examples{
\dontrun{
# Generate attributes for all valid registrations
assign_attributes()

# Generate attributes for a custom sample
assign_attributes(sample = rlang::exprs(created >= "2024-01-01"))
}

}
\seealso{
\link{SUPPORTED_ATTRIBUTES}, \code{\link[=get_registration_funder]{get_registration_funder()}}, \code{\link[=get_registration_institution]{get_registration_institution()}}
}
