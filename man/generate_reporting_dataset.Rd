% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.r
\name{generate_reporting_dataset}
\alias{generate_reporting_dataset}
\title{Generate a complete long-form reporting dataset}
\usage{
generate_reporting_dataset(
  subgroup_vars = cosr::SUPPORTED_ATTRIBUTES,
  los_ts_summary_path = "data/los_ts_overall.parquet",
  data_dir = "data"
)
}
\arguments{
\item{subgroup_vars}{Character vector of attribute names to include.
Defaults to \link{SUPPORTED_ATTRIBUTES}. Each must have a corresponding
\verb{data/los_ts_<attribute>.parquet} file generated by
\code{\link[=los_summary_by_attributes_all]{los_summary_by_attributes_all()}}.}

\item{los_ts_summary_path}{Path to the aggregate LOS time series summary
Parquet file (default: \code{"data/los_ts_overall.parquet"}).}

\item{data_dir}{Directory containing per-attribute Parquet files
(default: \code{"data"}).}
}
\value{
A tidy long-form data frame with columns:
\itemize{
\item \code{attribute}: the subgroup dimension (\code{"overall"} or attribute name)
\item \code{subgroup}: the subgroup value (e.g., institution name, \code{NA} for overall)
\item \code{date}: the snapshot date
\item \code{metric}: the metric name (prefixes stripped, e.g., \code{"osr"}, \code{"outputs"})
\item \code{moment}: one of \code{"value"}, \code{"lag"}, \code{"delta"}, \code{"growth"}
\item \code{value}: the numeric value
\item \code{value_type}: \code{"count"} or \code{"proportion"}
}
}
\description{
This is the main reporting workflow function. It generates a tidy, long-form
dataset combining aggregate (overall) summaries and per-attribute subgroup
summaries into a single data frame suitable for reporting and visualization.
}
\details{
For each attribute in \code{subgroup_vars}, the function reads the corresponding
pre-computed Parquet file (e.g., \code{data/los_ts_institution.parquet}), applies
\code{\link[=summary_longform]{summary_longform()}} with the attribute as a grouping variable, and appends
the result to the aggregate summary. An \code{attribute} column is added to
identify the subgroup dimension of each row (\code{"overall"} for aggregate rows).

The result is passed through \code{\link[=generate_value_types]{generate_value_types()}} to add a \code{value_type}
column and normalize metric names.
}
\examples{
\dontrun{
# Generate full reporting dataset for all attributes
report_df <- generate_reporting_dataset()

# Generate for a subset of attributes
report_df <- generate_reporting_dataset(
  subgroup_vars = c("funded", "institution")
)

# Save to disk
arrow::write_parquet(report_df, "data/reporting_dataset.parquet")
}

}
\seealso{
\code{\link[=summary_longform]{summary_longform()}}, \code{\link[=generate_value_types]{generate_value_types()}},
\code{\link[=los_summary_by_attributes_all]{los_summary_by_attributes_all()}}, \link{SUPPORTED_ATTRIBUTES}
}
