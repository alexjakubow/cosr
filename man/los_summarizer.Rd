% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.r
\name{los_summarizer}
\alias{los_summarizer}
\title{Generic summarizer for LOS time series data}
\usage{
los_summarizer(tbl, lazy = TRUE, threshold = 0, ...)
}
\arguments{
\item{tbl}{The input time series dataset (e.g., \code{data/los_ts.parquet})}

\item{lazy}{Whether to return a lazy query (default: TRUE) or collect
results into a data frame (default: FALSE)}

\item{threshold}{A numeric value between 0 and 1 specifying the minimum
proportion of total OSRs required for a subgroup to be included in the
summary (default: 0, meaning no threshold filter is applied)}

\item{...}{One or more unquoted expressions specifying the subgroup
variables to summarize by (e.g., \code{institution}, \code{funded}, \code{provider})}
}
\value{
An (un)lazy data frame containing summary statistics for each
subgroup, including:
\itemize{
\item n_osr: Number of registrations in subgroup
\item prop_osr: Proportion of all registrations
\item have_resources/outputs/outcomes: Counts of registrations with resources
\item prop_resources/outputs/outcomes/los: Proportions
}
}
\description{
This is a flexible function for computing summary statistics (counts,
proportions) for subgroups in your time series data. It can group by
any combination of variables and apply an optional threshold filter.
}
\examples{
\dontrun{
# Summarize by institution
ts_data <- arrow::open_dataset("data/los_ts.parquet")
los_summarizer(ts_data, institution, lazy = FALSE)

# Summarize by multiple attributes with threshold
los_summarizer(ts_data, institution, date, 
               threshold = 0.01, lazy = FALSE)
}

}
