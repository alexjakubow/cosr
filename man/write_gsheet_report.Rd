% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.r
\name{write_gsheet_report}
\alias{write_gsheet_report}
\title{Write annual LOS indicator reports to a Google Sheet}
\usage{
write_gsheet_report(
  annual_list,
  sheet_name = "LOS Indicators on OSF",
  sheet_id = NULL,
  token = NULL
)
}
\arguments{
\item{annual_list}{Named list of data frames as returned by
\code{\link[=format_gsheet_annual]{format_gsheet_annual()}}.}

\item{sheet_name}{Title of the Google Sheet to create or update
(default: \code{"LOS Indicators on OSF"}).}

\item{sheet_id}{Google Sheet ID or URL to update an existing sheet.
If \code{NULL} (default) a new sheet is created.}

\item{token}{Optional OAuth token or path to a service-account JSON file
passed to \code{googlesheets4::gs4_auth()}. If \code{NULL}, uses the currently
cached credentials.}
}
\value{
The Google Sheet ID (invisibly).
}
\description{
Takes the output of \code{\link[=format_gsheet_annual]{format_gsheet_annual()}} and writes each year as its own
tab in a Google Sheet. If the sheet does not yet exist it is created; if it
already exists the relevant tabs are overwritten.
}
\details{
New year tabs are added to the leftmost position, so the most recent year
appears first when the sheet is opened. Existing tabs retain their position.

The first column (Indicator labels) is automatically resized to fit content.

Authentication is handled by \code{googlesheets4}. Run
\code{googlesheets4::gs4_auth()} interactively before calling this function for
the first time, or supply a service-account token via the \code{token} argument.
}
\examples{
\dontrun{
# Authenticate (once per session)
googlesheets4::gs4_auth()

# Generate, format, and write
report_df <- generate_reporting_dataset()
annual    <- format_gsheet_annual(report_df)
write_gsheet_report(annual, sheet_name = "LOS Indicators on OSF")

# Update existing sheet by ID
write_gsheet_report(annual, sheet_id = "your-sheet-id-here")
}

}
\seealso{
\code{\link[=format_gsheet_annual]{format_gsheet_annual()}}, \code{\link[=generate_reporting_dataset]{generate_reporting_dataset()}}
}
