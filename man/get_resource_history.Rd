% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute.r
\name{get_resource_history}
\alias{get_resource_history}
\title{Get resource linkage history for registrations}
\usage{
get_resource_history(
  sample = cosr::expr_valid_regs,
  cutoff = Sys.Date(),
  lazy = TRUE
)
}
\arguments{
\item{sample}{The sample of registrations to analyze as an \code{rlang} expression
to be defused and evaluated in the context of the \code{osf_abstractnode} table.
Defaults to \code{cosr::expr_valid_regs}.}

\item{cutoff}{The cutoff date for including resource history events
(default: \code{Sys.Date()}). Only resources linked or removed on or before
this date will be included in the results.}

\item{lazy}{Whether to return a lazy query (default: TRUE) or collect
results into a data frame (default: FALSE)}
}
\value{
A data frame (or lazy query) with columns:
\itemize{
\item node_id: Registration identifier
\item artifact_id: Unique artifact identifier
\item artifact_type: Type code (1, 11, 21, 31, 41)
\item artifact_created: Timestamp when artifact was linked
\item artifact_deleted: Timestamp when artifact was removed (NA if still linked)
}
}
\description{
This is a low-level function that retrieves the complete history of
resources (data, code, materials, papers, supplements) linked to and
removed from registrations up until a specified cutoff date.
}
\details{
This function combines data from multiple OSF database tables to create
a comprehensive dataset of resource history. Most users should use
\code{\link[=resource_counts_at_date]{resource_counts_at_date()}} instead, which processes this raw history
into summary statistics.

\strong{Artifact Types:}
\itemize{
\item Type 1, 11, 21, 41: Outputs (data, code, materials, supplements)
\item Type 31: Outcomes (preprints, papers)
}
}
\examples{
\dontrun{
# Get resource history for all valid registrations
history <- get_resource_history(
  sample = cosr::expr_valid_regs,
  cutoff = Sys.Date(),
  lazy = FALSE
)

# Get history for specific date
history_2024 <- get_resource_history(
  cutoff = "2024-01-01",
  lazy = FALSE
)
}

}
\seealso{
\code{\link[=resource_counts_at_date]{resource_counts_at_date()}}
}
