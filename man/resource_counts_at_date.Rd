% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.r
\name{resource_counts_at_date}
\alias{resource_counts_at_date}
\title{Count resources and compute LOS status at a specific cutoff date}
\usage{
resource_counts_at_date(
  sample = cosr::expr_valid_regs,
  cutoff = Sys.Date(),
  lazy = TRUE
)
}
\arguments{
\item{sample}{The sample of registrations to analyze as an \code{rlang} expression.
Defaults to \code{cosr::expr_valid_regs}.}

\item{cutoff}{The cutoff date for including resource history events
(default: \code{Sys.Date()}). Only resources linked or removed on or before
this date will be included.}

\item{lazy}{Whether to return a lazy query (TRUE) or collect results (FALSE).
Default: TRUE}
}
\value{
A data frame (or lazy query) with columns:
\itemize{
\item node_id: Registration identifier
\item linked_outputs/outcomes: Count of linked resources
\item removed_outputs/outcomes: Count of removed resources
\item first_linked_output/outcome: Timestamp of first linked resource
\item total_resources: Total count of all linked resources
\item net_resources/outputs/outcomes: Net counts (linked - removed)
\item is_los: Binary indicator (1 if LOS, 0 otherwise)
}
}
\description{
This function computes summary statistics for registrations at a specified
cutoff date, including counts of linked and removed resources (outputs and
outcomes) and binary LOS status.
}
\details{
This is typically used as the aggregation function in \code{\link[=event_history_monthly]{event_history_monthly()}},
but can also be called directly to analyze a single point in time.

\strong{LOS Definition:} A registration is "LOS" if it has:
\itemize{
\item At least one net output (data, code, materials, supplements)
\item At least one net outcome (publication)
}
}
\examples{
\dontrun{
# Get current LOS status
current_los <- resource_counts_at_date(
  sample = cosr::expr_valid_regs,
  cutoff = Sys.Date(),
  lazy = FALSE
)

# Get historical snapshot
jan_2024 <- resource_counts_at_date(
  cutoff = "2024-01-01",
  lazy = FALSE
)
}

}
\seealso{
\code{\link[=get_resource_history]{get_resource_history()}}, \code{\link[=event_history_monthly]{event_history_monthly()}}
}
