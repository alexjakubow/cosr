% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.r
\name{los_summary_by_attributes_all}
\alias{los_summary_by_attributes_all}
\title{Compute time series summaries by multiple subgroup variables (complete pipeline)}
\usage{
los_summary_by_attributes_all(
  subgroup_vars = cosr::SUPPORTED_ATTRIBUTES,
  los_ts_path
)
}
\arguments{
\item{subgroup_vars}{Character vector of attribute names to summarize
(default: all supported attributes)}

\item{los_ts_path}{Path to the LOS time series Parquet file}
}
\value{
Invisible NULL. Results are written to Parquet files:
\itemize{
\item data/los_ts_\if{html}{\out{<attribute>}}.parquet (one per attribute)
}
}
\description{
This is typically the THIRD STEP in a workflow. It's a wrapper function that
computes summary statistics for all supported attributes and saves them as
Parquet files.
}
\details{
This function requires that you've already run:
\enumerate{
\item assign_attributes() - to generate attribute files
\item event_history_monthly() - to generate time series data
}
}
\examples{
\dontrun{
# After running assign_attributes() and event_history_monthly():
los_summary_by_attributes_all(
  subgroup_vars = SUPPORTED_ATTRIBUTES,
  los_ts_path = "data/los_ts.parquet"
)

# Or just for specific attributes:
los_summary_by_attributes_all(
  subgroup_vars = c("funder", "institution"),
  los_ts_path = "data/los_ts.parquet"
)
}

}
\seealso{
\code{\link[=los_summary_by_attribute]{los_summary_by_attribute()}}, \link{SUPPORTED_ATTRIBUTES}
}
