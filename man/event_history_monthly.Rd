% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.r
\name{event_history_monthly}
\alias{event_history_monthly}
\title{Generate monthly event history dataset for a specified sample and date range}
\usage{
event_history_monthly(
  .start = "2023-01-01",
  .end = Sys.Date(),
  .sample = cosr::expr_valid_regs,
  .fn = cosr::resource_counts_at_date,
  .fileout
)
}
\arguments{
\item{.start}{The start date for the time series (default: "2023-01-01").}

\item{.end}{The end date for the time series (default: \code{Sys.Date()})}

\item{.sample}{The sample of registrations to analyze (default: \code{cosr::expr_valid_regs}).}

\item{.fn}{The function to apply for computing event counts at each cutoff date
(default: \code{cosr::resource_counts_at_date}).}

\item{.fileout}{The file path for the output Parquet file (e.g., "data/los_ts.parquet").}
}
\value{
Invisible NULL. A Parquet file is written containing monthly snapshots with:
\itemize{
\item node_id: Registration identifier
\item date: Month of snapshot
\item linked_outputs/outcomes: Count of linked resources
\item removed_outputs/outcomes: Count of removed resources
\item net_outputs/outcomes: Net resource counts
\item is_los: Binary LOS indicator
\item los_change: Change in LOS status (1 = gained, -1 = lost, 0 = no change)
}
}
\description{
This is typically the SECOND STEP in a workflow. It generates a monthly time
series dataset tracking LOS (Lifecycle Open Science) status for registrations
over time.
}
\details{
The function computes resource counts at each monthly cutoff date and tracks
changes in LOS status (gains, losses, no change).
}
\examples{
\dontrun{
# Generate monthly LOS time series for all valid registrations
event_history_monthly(
  .start = "2023-01-01",
  .end = Sys.Date(),
  .fileout = "data/los_ts.parquet"
)
}

}
\seealso{
\code{\link[=resource_counts_at_date]{resource_counts_at_date()}}, \code{\link[=set_timespan]{set_timespan()}}
}
