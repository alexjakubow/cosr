% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.r
\name{format_gsheet_annual}
\alias{format_gsheet_annual}
\title{Format the overall reporting dataset as a wide annual Google Sheet}
\usage{
format_gsheet_annual(report_df, years = NULL, pretty_numbers = TRUE)
}
\arguments{
\item{report_df}{The long-form reporting data frame produced by
\code{\link[=generate_reporting_dataset]{generate_reporting_dataset()}}. Only rows where \code{attribute == "overall"}
are used.}

\item{years}{Integer vector of years to generate sheets for. Defaults to all
years present in \code{report_df}.}

\item{pretty_numbers}{Logical. If \code{TRUE} (default), formats numbers for
readability: large integers get thousand separators (e.g., "123,456") and
proportions are converted to percentages with 2 decimal places (e.g., "1.23").
If \code{FALSE}, returns raw numeric values.}
}
\value{
A named list of data frames, one per year (e.g. \code{list("2024" = ..., "2025" = ...)}). Each data frame has a leading \code{Indicator} column and
columns \code{1}–\code{12} for each calendar month.
}
\description{
Builds a named list of data frames — one per year — each structured to match
the CSV template ("LOS Indicators on OSF - YYYY.csv"). Each data frame
contains three subtables (TOTALS, NET CHANGE, \% GROWTH) stacked vertically
with two blank separator rows between them. Columns are months 1–12.
}
\examples{
\dontrun{
report_df <- generate_reporting_dataset()
annual    <- format_gsheet_annual(report_df)
write_gsheet_report(annual)

# With raw numbers (no formatting)
annual_raw <- format_gsheet_annual(report_df, pretty_numbers = FALSE)
}

}
\seealso{
\code{\link[=write_gsheet_report]{write_gsheet_report()}}, \code{\link[=generate_reporting_dataset]{generate_reporting_dataset()}}
}
