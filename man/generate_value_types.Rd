% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.r
\name{generate_value_types}
\alias{generate_value_types}
\title{Generate value types based on metric names}
\usage{
generate_value_types(tbl)
}
\arguments{
\item{tbl}{A data frame containing a column named \code{metric} with metric names.}
}
\value{
A data frame with rows filtered to only counts and proportions, with
an additional \code{value_type} column (\code{"count"} or \code{"proportion"}) and metric
name prefixes stripped.
}
\description{
This function identifies metrics that represent counts (e.g., those starting
with \code{n_}, \code{are_}, \code{have_}) and proportions (e.g., those starting with
\code{prop_}) and adds a new column \code{value_type} to indicate the type of value.
Metric name prefixes are stripped so that counts and proportions for the
same underlying concept share the same \code{metric} value (e.g., both
\code{have_outputs} and \code{prop_outputs} become \code{outputs}).
}
\examples{
\dontrun{
summary_longform(los_ts_summary) |>
  generate_value_types()
}

}
\seealso{
\code{\link[=summary_longform]{summary_longform()}}, \code{\link[=generate_reporting_dataset]{generate_reporting_dataset()}}
}
